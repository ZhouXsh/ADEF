这个目录用于存放可视化函数脚本和可视化结果。

任务需求：
论文中需要两张可视化图，一张是不同方法的可视化效果对比，另一张是我的方法在域外数据上的不同情感类型的对比。

对于第一张图：
输入不同方法的单个指定视频路径，通过间隔采样的方式，将视频帧以网格排列，其中横轴是不同的方法名（需要标出），纵轴是采样的位置（但是不用显式标出来）。

对于第二张图：
输入不同情感的单个指定视频路径，通过间隔采样的方式，将视频帧以网格排列，其中横轴是不同的情感类型（需要标出），纵轴是采样的位置（但是不用显式标出来）。

需要分别写两个不同的脚本，脚本最上面需要加上中文的使用方法注释，其中的参数可以硬编码。