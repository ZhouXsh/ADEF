### 2026年6月25日01:31:24

原本的train.py
            loss_exp = loss_exp + loss_exp / 2
            loss_exp_v = loss_exp_v + loss_exp_v / 2.
            loss_exp_s = loss_exp_s + loss_exp_s / 2.

现在的的train_new.py
            loss_expression = loss_expression + loss_exp / 2
            loss_exp_vel = loss_exp_vel + loss_exp_v / 2.
            loss_exp_smooth = loss_exp_smooth + loss_exp_s / 2.

结果：唇形同步好像变好了一些，但是情感表现就下降了。
0. 20260625毕设去掉两层调制_修改train错误的loss累积

gpt的意见6 8 9.3 

### 2026年6月26日--2026年6月27日

#### emotion_dit_cfg.py + train_cfg.py
1. 纯净版CFG（即prev_audio也参与dropout）
2. target_theta也执行cfg
3. 对于dropout的exp，不计算emo_loss。
0. 20260625_CFG_clear_full分支情感损失

没什么情感表现

#### emotion_dit_cfg_clear.py + train_cfg.py
1. emotion_dit_cfg.py的基础上，将gpt写的纯净版CFG（即prev_audio也参与dropout）  改回   原本毕设的CFG
0. 20260627_又改回原本的cfg了_但是用了full分支情感损失和target

效果还是挺明显的

### 2026年6月27日

#### emotion_dit_level.py + train_level.py
1. 加入情感等级损失约束
0. 20260627_emotion_dit_level_condition

唇形不同步，感觉是方法的问题

### 2026年6月28日18:01:37  
核心思想：不使用调制，而是对于每一次扩散步，在所有的Transformer（仅噪声和音频）背后，单独加一个EmoAdapter用于加情感。

v1：最保守版本（20260628_v1_stepaware_hidden_adapter）
- 保留原 audio-motion TransformerDecoder + alignment_mask
- 去掉 emotion 调制 audio
- 在 Transformer 输出后加 zero-init EmotionHiddenAdapter
- emotion token 形状为 [B, K, C]，隐式子空间，不做 lip/non-lip 显式划分
结果：完全没有唇形同步，嘴巴异常乱动

v2：情感编码增强版本（20260628_v2_phaseaware_emotion_tokens）
- 保留 v1 拓扑
- emotion token 改成 coarse / dynamic / detail 三组 timestep phase-aware tokens
- high/mid/low denoising step 对应不同隐式情感子空间强度
结果：视频的第一帧表情非常夸张，后续几乎没有运动

v3：更强注入版本（20260628_v3_layerwise_phase_adapter）
- 使用 v2 的 phase-aware emotion tokens
- 将 TransformerDecoder 拆成 layer list
- 在最后几层 decoder 后插入 zero-init emotion adapter
- 用于探索情感是否需要逐层影响高层去噪表示
结果：几乎没有运动

### 2026年6月30日16:58:24
核心思想：使用两个条件并行交叉注意力，随后加权求和

v1：原始 emotion 编码 + 双路 cross-attention



v2：扩散三阶段情感编码 + 双路 cross-attention
v2 保留 v1 的双路 denoising 主干，但把 emotion memory 改成三组隐式 token：
    coarse tokens：前期更强，控制整体情感基调
    region/dynamic tokens：中期更强，控制中尺度情感动态
    detail tokens：后期更强，控制细节情感变化
实现上没有显式 lip / non-lip 划分，仍然是在隐式关键点空间里学习。三组 token 的 timestep gate 在代码中是保守式的，不会像上一版 v2 那样早期过强。
适合验证：
step-aware emotion token 是否能增强情感表现，同时避免早期过强导致首帧夸张

v3：DICE-style emotion bank + audio-aware retrieval
v3 是我重新设计的版本。它借鉴 DICE-Talk 的 emotion bank / retrieval 思路：DICE-Talk 的 EmotionModel 里先用 codebook 检索 emotion prior，再通过 QKV attention 融合 emotion prompt 和检索到的 emotion prior。
ADEF 版本里做了一个轻量化 EmotionBankEncoder：
    emo_index
    ↓
    class-specific learnable codebook
    ↓
    label-query retrieval
    ↓
    audio-query retrieval
    ↓
    emotion memory tokens
代码里每个 emotion class 有多个 prototype tokens，同时 audio_summary 可以从同一个 bank 里检索一个 prosody-aware emotion prototype。
适合验证：
比单个 emotion embedding 更有信息量的 emotion bank 是否能增强情感表达

#### 让他阅读了其他paper后

v4：Decoupled Audio-Emotion Motion Diffusion（其实和v1没有本质区别）
- 从原始 emotion_dit.py 的训练接口出发
- 不再使用 emotion 调制 audio
- noisy motion 作为 query
- audio memory 和 emotion memory 分开 cross-attention
- 使用 DICE-style 三分支 CFG：
  null → audio-only → audio+emotion
- emotion 编码仍然使用原始方式：
  emo_index → nn.Embedding → [B, 1, C]

v5：Orthogonal Emotion Motion Basis Bank
- 在 v4 基础上加入 emotion motion basis bank
- 每个 emotion 类别拥有一组 learnable basis tokens
- basis tokens 包含 coarse / dynamic / detail 三类隐式情感运动基
- diffusion timestep 控制三组 token 的强弱
- audio summary 查询 emotion basis，使情感随语音韵律变化
- denoiser 使用：
  local audio attention
  global audio attention
  emotion attention
- emotion update 会在 hidden space 中对 audio-local update 做正交化

v6：Stochastic Affective Motion Prior
- 在 v5 基础上加入 stochastic affective prior
- 根据 emotion id + audio summary + diffusion timestep 预测 Gaussian latent tokens
- 训练时 reparameterization sample
- 推理时随机采样
- latent tokens 拼接到 emotion memory 中

### 2026年7月14日16:18:43

cuda1：运行train_vasa.py   |  实验名：20260714_vasa_prev25_current100
方法：不用学习先前参考，改成dropout
效果：有待提升，但是工作量有点大

cuda2：运行train.py   |  实验名：20260714_emotion_dit_timestep_0714
方法：时间步调制在每一层Transformer中进行
效果：可以加，但是还需要eval验证                     ++++++++++++++++++
结论：加

cuda3：运行train.py   |   实验名：20260714_emotion_dit_youhua_0714
方法：毕设的方法，进行优化
效果：情感表现反而变弱了
结论：不优化

### 2026年7月15日15:53:22

cuda1: 运行train.py   |   实验名：20260715_emotion_dit_youhua_0715
方法：毕设的方法，进行优化
效果：情感表现基本没了
结论：不优化

cuda2: 运行train_frame_0715.py  |  实验名：20260715_emotion_dit_frame_0715
方法：使用label调制frame-level，然后音频、frame-level并行。
效果：还没看，但是训练曲线很难看

### 2026年7月17日15:22:37

1. 训练时扩散步数由50改成500或1000，更细粒度。推理时保持50。

cuda1：运行 python train.py --n_diff_steps 500 |  实验名：20260717_emotion_dit_500stepDiffusion   
      模型：emotion_dit
方法：直接在毕设的基础上，将扩散步数由50改成500。（训练时间会延长）（不过效果可能不是很明显）
起始时间：2026年7月17日15:50:40
效果：看不出来
结论：采用

2. 将音频和情感合并成同一个条件，三CFG改成二CFG

cuda2：运行train.py    |  实验名： 20260717_emotion_dit_SingleCFG_0717   
      模型：emotion_dit_SingleCFG_0717
方法：直接在毕设的基础上，将三CFG改成二CFG，0.1概率dropout。（单一变量，扩散步还是50）
起始时间：2026年7月17日16:29:53
效果：情感表现更好，应该是dropout概率变低导致的。
结论：使方法更简便了，采用

cuda3：运行train.py    |  实验名： 20260717_emotion_dit_SingleCFG_0717_youhua
      模型：emotion_dit_SingleCFG_0717_youhua
方法：上述的基础上，历史音频也属于这个联合条件；它应当使用相同 mask 一起 dropout，而不是单独随机屏蔽。
起始时间：2026年7月17日16:47:19
效果：不优化的效果更好
结论：不采用

3. 音频特征具有感受野，不要使用严格的对角线mask对齐
效果：
结论：

4. 将第一帧复制n_motions长度，与噪声进行拼接，就像端到端的那样。。。。？
效果：
结论：不用复制，直接把第一帧和规范关键点拼在一起

5. 目前表情是高斯分布归一化，而其他姿态是0-1归一化，需不需要改成一致的呢？
效果：
结论：先改了再说

6. 在DiTs的结尾将一个motion_dec改成两个，一个负责音频相关，一个负责情感相关。但问题还是无法显式区分嘴唇区域的关键点。。。。

cuda4：运行train.py    |  实验名： 20260717_emotion_dit_0717_2MotionDecoder
      模型：emotion_dit_0717_2MotionDecoder
方法：直接在毕设的基础上，新增emotion_delta = self.emotion_dec(feat_out)，从而实现对emotion的预测，进而实现对motion的增强
效果：没啥效果
结论：不需要


### 2026年7月20日16:04:14

1. 编码大一统的方法 
cuda2:  运行 train.py --n_diff_steps 500  |  实验名： 20260720_emotion_dit_Unification
      模型：emotion_dit_Unification.py
方法：
1.在《模型：emotion_dit_SingleCFG_0717》的基础上，新增《emotion_dit_timestep_0714》的时间步调制。
2.pitch / yaw / roll / scale / t 改成exp同款的正态分布归一化与反归一化。
3.训练扩散步由50->500。

train.py里面写的：
from src.modules.emotion_dit_Unification import DitTalkingHead
g_exp_name = "20260720_emotion_dit_Unification"
device_id = 2

效果：损失唇形同步，双重验证了，还是损失。。。

--------------------------------------------

2. 大一统方法验证mask
cuda3:  运行 train.py --n_diff_steps 500 --align_mask_width 2
同上，仅验证align_mask_width。
train.py里面写的：

from src.modules.emotion_dit_Unification import DitTalkingHead
g_exp_name = "20260720_emotion_dit_Unification_width2"
device_id = 3

效果：唇形同步正常

与1对比，难道一个“--align_mask_width 2”会引起这么大变化？  还真是！

### 2026年7月21日22:41:30

1. 无cfg：

cuda1:  运行 train.py --n_diff_steps 500 --align_mask_width 2

train.py里面写的：

from src.modules.emotion_dit_Unification_noCFG_0721 import DitTalkingHead
g_exp_name = "20260721_emotion_dit_Unification_noCFG_0721"
device_id = 1

效果：唇形不同步，表情不自然
结论：必须要cfg。

2. 将frame-level用于noise的调制，然后自注意力：

cuda2:      运行     train_framelevel_0721.py --n_diff_steps 500
            实验名   20260721_emotion_dit_Unification_framelevel_0721
            模型     emotion_dit_Unification_framelevel_0721
效果：
结论：

2. 学KDTalker，引入第一帧的信息：

cuda3:      运行    train_1pad_0721.py --n_diff_steps 500 --align_mask_width 2
            实验名  20260721_emotion_dit_Unification_1pad_0721
            模型    emotion_dit_Unification_1pad_0721
效果：唇形不同步，面部抖动严重
结论：不需要

1. 时间步调制的继续优化：




### 2026年7月23日19:59:16 不管这哪的了，全力优化emotion_dit_Unification！






20260724_emotion_dit_Unification_mask2_noCut